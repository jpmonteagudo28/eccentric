simulate_clt <- function(n = NULL,
                         inverse = NULL,
                         samples = 249){

  stopifnot(is.null(n),
            is.null(inverse),
            is.null(samples) || is.numeric(samples)
            )


}
